rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    // Allow read and write to all documents for seamless development and project transitions
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
